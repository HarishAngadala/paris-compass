# Copy this file to .env and add your Gemini API key.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
PORT=8080

# Optional: point to an official Global Footprint Network / FoDaFo CSV export.
# If omitted, the app uses the public compatibility dataset configured in ClimateDataService.
FOOTPRINT_CSV_PATH=
